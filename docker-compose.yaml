services:

  mysql:
    image: mysql
    container_name: mysql-project
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: q1w2e3
      MYSQL_DATABASE: catch_table
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: